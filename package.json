{
  "name": "damon-portfolio",
  "version": "0.1.0",
  "private": true,
  "//": [
    "comments here",
    "use `npm run deploy` to push to gh-pages repo (THIS IS NO LONGER ACCURATE",
    "https://medium.com/the-andela-way/how-to-deploy-your-react-application-to-github-pages-in-less-than-5-minutes-8c5f665a2d2a",
    "...",
    "to push live (on netlify host)",
    "simply push from the netlify-deploy branch",
    "work on master to avoid pushing to production",
    "ideally keep the two merged"
  ],
  "dependencies": {
    "prismic-javascript": "^2.1.1",
    "prismic-reactjs": "^1.1.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-ga": "^2.7.0",
    "react-scripts": "3.1.1",
    "styled-components": "^4.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.10.6"
  }
}
